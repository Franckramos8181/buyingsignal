# docs

Place the generated workflow diagram here as `workflow.png` — the top-level
[`README.md`](../README.md) embeds it.

To (re)generate it, feed the prompt in [`prompt.md`](../prompt.md) to an image
generator (Ideogram or Google Imagen recommended for label-heavy diagrams), then
save the result as `docs/workflow.png`.
